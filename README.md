🚗 DADDT Parking System (FiveM)
ระบบจอดรถส่วนตัวสำหรับเซิร์ฟเวอร์ FiveM (QBCore Framework) ที่ช่วยให้ผู้เล่นสามารถจอดรถไว้ ณ จุดใดก็ได้บนแผนที่ พร้อมระบบบันทึกสถานะ พิกัด และความปลอดภัยจากการถูกทำลายหรือถูกขโมย

📌 คุณสมบัติหลัก (Key Features)
Real-time Parking: บันทึกตำแหน่งรถล่าสุดลงฐานข้อมูล (Database) แบบเรียลไทม์

Persistent State: รถที่จอดไว้จะถูกล็อก (Lock), แช่แข็งตำแหน่ง (Freeze), และเป็นอมตะ (Invincible) เพื่อป้องกันการหายหรือโดนชน

Vehicle Data Sync: บันทึกข้อมูลละเอียด ทั้งค่าน้ำมัน (Fuel), ความแรงเครื่องยนต์ (Engine), ความเสียหายตัวถัง (Body) และของแต่งรถ (Mods)

GPS Tracking: หากผู้เล่นอยู่ห่างจากรถ สามารถสั่งปักหมุด (Waypoint) ผ่านเมนูเพื่อนำทางไปยังรถได้

My Vehicles Menu: ระบบ UI สวยงาม (ox_lib) สำหรับดูรายการรถที่จอดอยู่ทั้งหมด

Ownership Check: ระบบตรวจสอบความเป็นเจ้าของ ป้องกันไม่ให้จอดรถ NPC หรือรถที่ไม่ได้เป็นเจ้าของ

🛠 ข้อมูลทางเทคนิค (Technical Stack)
Framework: QBCore

UI Library: ox_lib

Database: oxmysql / mysql-async

Dependencies: qb-core, ox_lib, PolyZone (สำหรับระบบโซน)

📂 โครงสร้างไฟล์ (File Structure)
ไฟล์	หน้าที่หลัก
config.lua	ตั้งค่าโหมด Debug และประเภทการแจ้งเตือน (Notify)
fxmanifest.lua	กำหนด Metadata และการโหลด Script
client/main.lua	จัดการ Logic ฝั่งผู้เล่น, เมนู UI และการ Spawn รถ
server/main.lua	จัดการการเชื่อมต่อฐานข้อมูล SQL และการบันทึกสถานะ
🎮 วิธีการใช้งาน (Commands)
1. /parking (หรือผ่านเมนู F6)
ใช้ขณะนั่งอยู่ในรถที่ตำแหน่งคนขับ เพื่อทำการ "จอด"

ระบบจะตรวจสอบว่าเป็นรถส่วนตัวหรือไม่

ทำการหยุดความเร็วรถและบันทึกพิกัด

ล็อคประตูและป้องกันความเสียหาย

2. /unparking
ใช้เมื่อต้องการ "ปลดล็อก" การจอดของรถคันที่อยู่ตรงหน้า

คืนสถานะฟิสิกส์ให้รถขับเคลื่อนได้ตามปกติ

อัปเดตสถานะในฐานข้อมูลเป็น state 0 (อยู่นอกโรงรถ)

3. /myvehicles (หรือผ่านเมนู F6)
เปิดเมนูดูรายการรถทั้งหมดของผู้เล่น

หากอยู่ไกล: ระบบจะสร้าง Blip (ไอคอน) และปักหมุด GPS ให้ที่แผนที่

หากอยู่ใกล้: สามารถเลือกเพื่อเรียกใช้รถ (Spawn) ออกมาในจุดที่จอดไว้

4. [F6]
ปุ่มลัดมาตรฐานสำหรับเปิด Parking Main Menu (สามารถเปลี่ยนได้ใน Key Bindings ของตัวเกม)

💾 ข้อมูลที่ถูกบันทึกใน Database (SQL)
สคริปต์นี้จะอัปเดตข้อมูลในตาราง player_vehicles ดังนี้:

parking: ข้อมูล JSON รวมทั้งหมด

coords: พิกัด (X, Y, Z)

rotation: องศาการหันของรถ

state: สถานะการจอด (1 = จอดข้างทาง)

fuel, engine, body: สภาพของรถล่าสุด

⚠️ คำแนะนำสำหรับการติดตั้ง
ตรวจสอบให้มั่นใจว่าในฐานข้อมูล player_vehicles มี Column ดังต่อไปนี้:

coords (text/longtext)

rotation (text/longtext)

parking (longtext - ถ้ามีตาม Logic เก่า)

ตรวจสอบว่ามีการใช้ ox_lib ในเซิร์ฟเวอร์แล้ว เนื่องจากเมนูและ Progress Bar ทั้งหมดรันผ่าน Library นี้

Author: DADDT

Version: 0.1.0

License: Private / FiveM Server Community
